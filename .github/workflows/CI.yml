name: CI

on: push

jobs:
  build:
    name: Continuous Integration Flow
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
       
      - name: Install modules
        run: npm install
        
      - name: Run tests
        run: npm run tests
